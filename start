#!/bin/bash
export DBUS_SYSTEM_BUS_ADDRESS=unix:path=/var/run/dbus/system_bus_socket ./wifi-connect --clear=false --portal-interface=wlp2s0

echo "wifi connect exited"
while true; do
    sleep 100
done

